import React from 'react';
import PlaceholderPage from '../../components/common/PlaceholderPage';

const StrategyStudio: React.FC = () => (
  <PlaceholderPage
    title="Strategy Studio"
    description="Build and refine GEO playbooks, intent clusters, and tactical matrices from your campaign brief. Select, edit, and confirm strategies before production."
    phase="v1"
  />
);

export default StrategyStudio;

import React from 'react';
import PlaceholderPage from '../../components/common/PlaceholderPage';

const Campaigns: React.FC = () => (
  <PlaceholderPage
    title="Campaigns"
    description="Browse, manage, and track all active and archived campaigns. Each campaign links its brief, strategy playbooks, and produced content assets."
    phase="v1"
  />
);

export default Campaigns;

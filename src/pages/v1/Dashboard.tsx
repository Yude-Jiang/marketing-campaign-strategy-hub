import React from 'react';
import PlaceholderPage from '../../components/common/PlaceholderPage';

const Dashboard: React.FC = () => (
  <PlaceholderPage
    title="Dashboard"
    description="Campaign performance overview, KPI snapshot, recent activity, and quick actions across all active campaigns."
    phase="v1"
  />
);

export default Dashboard;
